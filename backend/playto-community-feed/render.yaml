services:
  - type: web
    name: pulsefeed-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python manage.py migrate && python manage.py seed_demo && gunicorn backend.wsgi:application
